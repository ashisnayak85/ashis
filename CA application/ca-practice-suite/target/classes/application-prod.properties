# MySQL - real deployment. Set these via environment variables, never commit
# real credentials.
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/ca_practice_suite?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
